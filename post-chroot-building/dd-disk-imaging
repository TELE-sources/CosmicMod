****

# For creating disk image then image to disk

fdisk -l

dd if=/dev/sdc conv=sync,noerror bs=64K | gzip -c  > /home/mod/CosmicMod-2021.img.gz

gunzip -c /home/mod/CosmicMod-2021.img.gz | dd of=/dev/sda


****

# For disk to disk

dd if=/dev/sda of=/dev/sdb bs=64K conv=noerror,sync status=progress

**** 
